# Homebrew Tap for Slugged

Install `slugged` via Homebrew:

```bash
brew tap gallo-s-chingon/slugged
brew install slugged

see [https://github.com/gallo-s-chingon/slugged](gallo-s-chingon/slugged) for details
